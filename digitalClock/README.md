# Digital Clock

This is a simple digital clock built using C. It displays the current system time and updates every second.

## 💡 How to Compile and Run

1. Open terminal and navigate to the project folder:
   ```bash
   cd 01_Digital_Clock
   ```

2. Compile the program:
   ```bash
   gcc clock.c -o clock
   ```

3. Run it:
   ```bash
   ./clock
   ```